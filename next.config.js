/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: [
			"upload.wikimedia.org",
			"tailwindui.com",
			"images.unsplash.com",
			"www.presuntoinocente.com",
			"cdn.pixabay.com",
		],
	},
};

module.exports = nextConfig;
